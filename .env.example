# Environment configuration (do not commit secrets)
# This project is a static site; environment variables are typically provided via config.local.js
# For reference only, mirror keys here and copy them to config.local.js instead of committing actual values.

# Example values (DO NOT COMMIT REAL SECRETS)
N8N_WEBHOOK_TEST="https://example/webhook-test/UUID"
N8N_WEBHOOK_PROD="https://example/webhook/UUID"
N8N_AUTH_HEADER="Authorization: Bearer {{TOKEN}}"

